input GetUserInput {
  id: ID!
}

type GetUserResult {
  user: User
}

extend type Query {
  getUser(input: GetUserInput!): GetUserResult!
}

type User {
  id: ID!
  name: String!
  email: String!
  posts: [Post!]
}
